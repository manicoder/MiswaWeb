#✅ 🔧 Railway Variables for Backend (MltAdminApi)
# ASP.NET Core settings
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:$PORT

# JWT Authentication
JWT_KEY=your-super-secret-jwt-key-here-256-bits-minimum

# Encryption key for sensitive data
MLT_ENCRYPTION_KEY=your-32-character-encryption-key-here

# Frontend base URL for CORS and redirection
Frontend__BaseUrl=https://mltadmin.mylittletales.com #removed

# Migration flags
ENABLE_MIGRATIONS=true #removed
APPLY_MIGRATIONS=true #removed

# Database connection (set automatically by Railway when adding PostgreSQL)
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Optional: Custom port if needed (Railway usually maps automatically)
PORT=5000


#######################################################
# ✅ 🔧 Railway Variables for Frontend (MltAdminWeb)
NODE_ENV=production #removed
VITE_NODE_ENV=production #removed
VITE_API_URL=https://mltadminapi.mylittletales.com/api  